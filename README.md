Welcome! Nothing much here yet.

This project is a part of webGL training leading to creation of simple web 3D editor.

`git clone`,

`docker-compose build`, then
`docker-compose up` and `docker compose down`
